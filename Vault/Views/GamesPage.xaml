<UserControl x:Class="Vault.Views.GamesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Vault.Views"
      Background="#1a1a2e">

    <UserControl.Resources>
        <Style x:Key="FilterButton" TargetType="Button">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="#b2bec3"/>
            <Setter Property="BorderBrush" Value="#2d3561"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2d3561"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterButtonActive" TargetType="Button" BasedOn="{StaticResource FilterButton}">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderBrush" Value="#e94560"/>
        </Style>

        <!-- Invisible button overlay style for game tiles -->
        <Style x:Key="TileButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter bar -->
        <Border Grid.Row="0" Background="#16213e"
                BorderBrush="#2d3561" BorderThickness="0,0,0,1"
                Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="FILTER:" Foreground="#636e72" FontSize="11"
                               FontFamily="Segoe UI" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button x:Name="BtnAll" Content="All" Style="{StaticResource FilterButtonActive}"
                            Click="FilterBtn_Click" Tag="All"/>
                    <Button x:Name="BtnPlaying" Content="Playing" Style="{StaticResource FilterButton}"
                            Click="FilterBtn_Click" Tag="Playing"/>
                    <Button x:Name="BtnCompleted" Content="Completed" Style="{StaticResource FilterButton}"
                            Click="FilterBtn_Click" Tag="Completed"/>
                    <Button x:Name="BtnNotStarted" Content="Not Started" Style="{StaticResource FilterButton}"
                            Click="FilterBtn_Click" Tag="Not Started"/>
                    <Button x:Name="BtnDownloaded" Content="Downloaded" Style="{StaticResource FilterButton}"
                            Click="FilterBtn_Click" Tag="Downloaded"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="SORT:" Foreground="#636e72" FontSize="11"
                               FontFamily="Segoe UI" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="SortCombo" Width="140" Height="30"
                              FontFamily="Segoe UI" FontSize="12"
                              SelectionChanged="SortCombo_SelectionChanged">
                        <ComboBox.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#16213e"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#b2bec3"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#16213e"/>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Background" Value="#0d1117"/>
                                <Setter Property="Foreground" Value="#b2bec3"/>
                                <Setter Property="FontFamily" Value="Segoe UI"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBoxItem">
                                            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsHighlighted" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#e94560"/>
                                                    <Setter Property="Foreground" Value="#ffffff"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#2d3561"/>
                                                    <Setter Property="Foreground" Value="#ffffff"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.Resources>
                        <ComboBox.Template>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <Border x:Name="MainBorder" Background="#16213e"
                                            BorderBrush="#2d3561" BorderThickness="1" CornerRadius="6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="28"/>
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0"
                                                              Content="{TemplateBinding SelectionBoxItem}"
                                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                              VerticalAlignment="Center" Margin="10,0,0,0"
                                                              IsHitTestVisible="False"
                                                              TextBlock.Foreground="#b2bec3"/>
                                            <TextBlock Grid.Column="1" Text="&#x25BE;"
                                                       Foreground="#b2bec3" VerticalAlignment="Center"
                                                       HorizontalAlignment="Center" FontSize="10"/>
                                            <ToggleButton Grid.ColumnSpan="2"
                                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                          Focusable="False" Opacity="0">
                                                <ToggleButton.Template>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="Transparent"/>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                        </Grid>
                                    </Border>
                                    <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                           Placement="Bottom" AllowsTransparency="False">
                                        <Border Background="#0d1117" BorderBrush="#2d3561" BorderThickness="1"
                                                MinWidth="{TemplateBinding ActualWidth}">
                                            <ScrollViewer MaxHeight="200">
                                                <ItemsPresenter/>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="MainBorder" Property="Background" Value="#e94560"/>
                                        <Setter TargetName="MainBorder" Property="BorderBrush" Value="#e94560"/>
                                    </Trigger>
                                    <Trigger Property="IsDropDownOpen" Value="True">
                                        <Setter TargetName="MainBorder" Property="Background" Value="#e94560"/>
                                        <Setter TargetName="MainBorder" Property="BorderBrush" Value="#e94560"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ComboBox.Template>
                        <ComboBoxItem Content="Title A-Z" IsSelected="True"/>
                        <ComboBoxItem Content="Title Z-A"/>
                        <ComboBoxItem Content="Year (Newest)"/>
                        <ComboBoxItem Content="Year (Oldest)"/>
                        <ComboBoxItem Content="Platform"/>
                        <ComboBoxItem Content="Playtime"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content: sidebar + games + detail panel -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="DetailColumn" Width="0"/>
            </Grid.ColumnDefinitions>

            <!-- Platform sidebar -->
            <Border Grid.Column="0" Background="#16213e"
                    BorderBrush="#2d3561" BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PlatformPanel" Margin="0,8,0,8"/>
                </ScrollViewer>
            </Border>

            <!-- Games area -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TxtGameCount" Grid.Row="0"
                           Foreground="#636e72" FontSize="12"
                           FontFamily="Segoe UI" Margin="16,8"/>

                <!-- Grid view -->
                <ScrollViewer Grid.Row="1" x:Name="GridScrollViewer"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="GamesItemsControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!-- Button wraps the whole tile — this is the reliable WPF click pattern -->
                                <Button Style="{StaticResource TileButton}"
                                        Click="GameTile_Click"
                                        Width="150" Height="210" Margin="6">
                                    <Border CornerRadius="8" ClipToBounds="True"
                                            Background="#16213e">
                                        <Grid>
                                            <Image Source="{Binding BoxArtPath}" Stretch="UniformToFill"/>
                                            <Border Background="#0f3460">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasBoxArt}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Title}" Foreground="#b2bec3"
                                                           FontFamily="Segoe UI" FontSize="12"
                                                           TextWrapping="Wrap" TextAlignment="Center"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" Margin="8"/>
                                            </Border>
                                            <Border VerticalAlignment="Bottom" Height="70">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Color="Transparent" Offset="0"/>
                                                        <GradientStop Color="#E616213e" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                            </Border>
                                            <StackPanel VerticalAlignment="Bottom" Margin="8,0,8,8">
                                                <Border Background="{Binding PlatformColor}"
                                                        CornerRadius="3" Padding="5,1"
                                                        HorizontalAlignment="Left" Margin="0,0,0,4">
                                                    <TextBlock Text="{Binding PlatformShort}"
                                                               Foreground="White" FontSize="9"
                                                               FontWeight="Bold" FontFamily="Segoe UI"/>
                                                </Border>
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="7" Height="7" Margin="0,0,4,0"
                                                             VerticalAlignment="Center"
                                                             Fill="{Binding StatusColor}"/>
                                                    <TextBlock Text="{Binding Status}"
                                                               Foreground="#b2bec3" FontSize="10"
                                                               FontFamily="Segoe UI"
                                                               VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <Border Background="#B4000000">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDownloaded}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="⬇ Not Downloaded"
                                                           Foreground="#636e72" FontSize="11"
                                                           FontFamily="Segoe UI"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- List view -->
                <ListView Grid.Row="1" x:Name="GamesListView"
                          Background="Transparent" BorderThickness="0"
                          Foreground="#ffffff" FontFamily="Segoe UI"
                          Visibility="Collapsed"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Title" Width="300" DisplayMemberBinding="{Binding Title}"/>
                            <GridViewColumn Header="Platform" Width="120" DisplayMemberBinding="{Binding Platform}"/>
                            <GridViewColumn Header="Year" Width="60" DisplayMemberBinding="{Binding Year}"/>
                            <GridViewColumn Header="Status" Width="100" DisplayMemberBinding="{Binding Status}"/>
                            <GridViewColumn Header="Playtime" Width="90" DisplayMemberBinding="{Binding PlaytimeDisplay}"/>
                            <GridViewColumn Header="Size" Width="80" DisplayMemberBinding="{Binding SizeDisplay}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>

            <!-- Detail panel -->
            <Border Grid.Column="2" BorderBrush="#2d3561" BorderThickness="1,0,0,0">
                <local:GameDetailPanel x:Name="DetailPanel" Visibility="Collapsed"/>
            </Border>
        </Grid>

        
        <Border Grid.Row="1" x:Name="LoadingOverlay"
                Background="#CC1a1a2e" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Loading your library..." Foreground="#ffffff"
                           FontSize="18" FontFamily="Segoe UI"
                           HorizontalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Margin="0,16,0,0" Foreground="#e94560" Background="#2d3561"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
